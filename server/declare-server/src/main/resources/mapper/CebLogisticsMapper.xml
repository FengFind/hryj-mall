<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hryj.mapper.CebPaymentMapper">

    <!--根据编号查询-->
    <select id="findByPayNo" parameterType="java.lang.String" resultType="com.hryj.entity.bo.declare.CebPayment">
        SELECT * FROM dec_ceb_payment  WHERE  pay_no = #{payNo}
    </select>

</mapper>